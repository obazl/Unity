load("@bazel_skylib//rules:common_settings.bzl",
     "bool_flag", "string_flag")

# UNITY_INCLUDE_DOUBLE
bool_flag(name = "no_extras", build_setting_default = False)
config_setting(name = "no_extras?",
               flag_values = {":no_extras": "True"})
